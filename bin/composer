#!/bin/bash +x

ROOT="$( cd "$( dirname "${BASH_SOURCE[0]}" )/../" && pwd )"

. "${ROOT}"/bin/lib/env.sh
. "${ROOT}"/bin/lib/tty.sh

docker run \
    -i${TTY} \
    --rm \
    -v "$(pwd)":/data -w /data \
	--net ${PROJECT_NAME}_default \
	composer:1.8 "$@"
